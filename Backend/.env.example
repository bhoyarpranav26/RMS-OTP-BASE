# Example environment variables for the Backend service
# DO NOT commit real secrets to git. Copy this file to .env and fill values locally or in Render.

# Server
PORT=5000

# MongoDB (replace with your Atlas connection string)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/rms?retryWrites=true&w=majority

# JWT
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=7d

# Frontend URL (used for CORS)
FRONTEND_URL=https://your-frontend-name.onrender.com
CORS_ORIGINS=https://your-frontend-name.onrender.com

# Email / SendGrid
# Preferred: use SendGrid Web API key
SENDGRID_API_KEY=SG.xxxxxxxx_your_sendgrid_api_key_xxxxxx
FROM_EMAIL=your-verified-sender@example.com

# Fallback Gmail SMTP (only if you want Gmail fallback)
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_PASS=your_gmail_app_password

# For local testing only (set to true to skip real emails and return OTP in responses)
SKIP_EMAIL=true
